# Postgres connection string from Neon
DATABASE_URL=postgres://user:password@host/dbname

# For Neon you usually need SSL for Render
DB_SSL=true

# Base URL of deployed app (for future use if needed)
BASE_URL=https://your-tinylink.onrender.com

# Port (Render automatically sets PORT)
PORT=3000
